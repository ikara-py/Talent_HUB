<h1>Recruiter Dashboard OK ✅</h1>

{% if jobs is defined and jobs is not empty %}
    <ul>
        {% for job in jobs %}
            <li>
                <strong>{{ job.title }}</strong><br>
                {{ job.description }}
            </li>
        {% endfor %}
    </ul>
{% else %}
    <p>No job offers yet.</p>
{% endif %}
