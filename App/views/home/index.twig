<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TalentHub - Home</title>
    <link rel="stylesheet" href="{{ base_url }}/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a href="{{ url() }}" class="logo">TalentHub</a>
            <div class="nav-links">
                {% if session.user_id %}
                    <span>Welcome, {{ session.user_full_name }}!</span>
                    <a href="{{ url('auth/logout') }}" class="btn btn-secondary">Logout</a>
                {% else %}
                    <a href="{{ url('auth/login') }}" class="btn btn-secondary">Login</a>
                    <a href="{{ url('auth/register') }}" class="btn btn-primary">Register</a>
                {% endif %}
            </div>
        </div>
    </nav>

    <main class="hero">
        <div class="container">
            <h1>Welcome to TalentHub</h1>
            <p>Connect talented candidates with amazing opportunities</p>
            
            {% if session.user_id %}
                <div class="dashboard-link">
                    {% if session.user_role == 'admin' %}
                        <a href="{{ url('admin/dashboard') }}" class="btn btn-large">Go to Dashboard</a>
                    {% elseif session.user_role == 'recruiter' %}
                        <a href="{{ url('recruiter/dashboard') }}" class="btn btn-large">Go to Dashboard</a>
                    {% elseif session.user_role == 'candidate' %}
                        <a href="{{ url('candidate/dashboard') }}" class="btn btn-large">Go to Dashboard</a>
                    {% endif %}
                </div>
            {% else %}
                <div class="cta-buttons">
                    <a href="{{ url('auth/register') }}" class="btn btn-large btn-primary">Get Started</a>
                    <a href="{{ url('auth/login') }}" class="btn btn-large btn-secondary">Login</a>
                </div>
            {% endif %}
        </div>
    </main>
</body>
</html>